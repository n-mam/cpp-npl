project (NPL)

find_package(Threads)

if (UNIX)
 find_package(OpenSSL REQUIRED)
endif (UNIX)

ADD_EXECUTABLE(Agent Agent.cpp)

SET_PROPERTY(TARGET Agent PROPERTY CXX_STANDARD 17)

TARGET_INCLUDE_DIRECTORIES(Agent PRIVATE INCLUDE)

TARGET_INCLUDE_DIRECTORIES(Agent PUBLIC .)

if (WIN32)
  TARGET_LINK_LIBRARIES(Agent Ws2_32.lib)
else()
  TARGET_LINK_LIBRARIES(Agent ${CMAKE_THREAD_LIBS_INIT} OpenSSL::SSL)
endif (WIN32)